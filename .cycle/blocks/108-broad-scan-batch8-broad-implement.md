---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: T-5 (deploy.sh gates on lint:gas too; missing eslint / playwright FAIL the gate via CI=1), T-7 (explicit OAuth scopes: dashboard drops `script.container.ui`; cdr-report + cdr-import gain derived explicit lists; new manifest-scopes.test.js pins both directions; dqe-report stays frozen), T-8 (ingest-watchdog + dqe-silence-watch pin a weekday clock instead of skipping on real weekends; a Saturday pin proves the gate), T-9 (drive-smoke asserts a positive drawn-canvas floor per page and inside the generated IR modal), A-4 (`sheetSafeCell_` on the Queue Report subscriber, Alert Config, Digest Config and Alert Log writers), A-5 (IR email: PNG magic bytes + 8 MB cap, one-line capped subject, the registered-address read failure surfaces), A-6 (the uncached Access Control read takes the script lock; serves-without-caching when it cannot)
Files modified: scripts/deploy.sh, apps-script/department-dashboard/appsscript.json, apps-script/cdr-report/appsscript.json, apps-script/cdr-import/appsscript.json, apps-script/department-dashboard/Auth.gs, apps-script/department-dashboard/IndividualReport.gs, apps-script/department-dashboard/Alerts.gs, apps-script/department-dashboard/Digest.gs, apps-script/department-dashboard/QueueReportEmail.gs, tools/ui-harness/drive-smoke.js, tests/harness/shim.js, tests/unit/manifest-scopes.test.js (new), tests/unit/ingest-watchdog.test.js, tests/unit/dqe-silence-watch.test.js, tests/unit/ir-send-to-agent.test.js, tests/unit/config-editor-c3.test.js, tests/unit/access-control-editor.test.js, tests/unit/cross-file-pins.test.js, tests/README.md, docs/module-dependencies.md, docs/operator-state.md, docs/fix-history.md, CLAUDE.md

CHANGES:
T-5 | scripts/deploy.sh, cross-file-pins.test.js, CLAUDE.md | Inside the DEPLOY_SKIP_CI gate: `npm run ci`, then `CI=1 npm run lint:gas`, then `CI=1 npm run ci:ui` -- the F-9 absence-fails branch of both tools now applies to the live push. Pin: the gate block carries all three with CI=1 and both tools keep their CI branch. Key-commands deploy block updated.
T-7 | three appsscript.json, manifest-scopes.test.js, operator-state #9 | Dashboard: container.ui removed (no getUi call). cdr-report: spreadsheets, container.ui, userinfo.email, scriptapp, send_mail, external_request, drive.readonly (the PDF export fetches the sheet's /export URL with the script's token). cdr-import: spreadsheets, container.ui, scriptapp, send_mail, external_request, drive (DriveApp). The suite derives the required set from service-call regexes per project and asserts declared == required (missing AND unused both fail); dqe-report (INV-22) pinned as auto-detect.
T-8 | ingest-watchdog.test.js, dqe-silence-watch.test.js | `fakeDate()` (the neon-keepwarm shape) pins `ctx.Date` to Wed 2026-06-03 for the whole watchdog suite; `atDate(SATURDAY)` proves the weekend gate skips and the same state on the weekday alerts; the silence-watch O-7 test runs under `atWeekday()`. No `t.skip` left.
T-9 | tools/ui-harness/drive-smoke.js | `drawnCanvasCount(page, within)` (visible, laid out, varied pixels) + `MIN_DRAWN` {overview:1, dept:2, escalations:0} recorded per page/role, and `>= 1` inside `#individual-modal` after Generate. Measured: overview 1, dept 3 (admin) / 2 (manager), IR 1.
A-4 | QueueReportEmail.gs, Alerts.gs (x2), Digest.gs, config-editor-c3.test.js | `sheetSafeCell_` on email/notes (subscriber row), recipients/notes/skip-dates (Alert Config row), email/notes (Digest Config row), recipients/triggeredBy/notes (Alert Log append). Validated enums / roster headers / numbers pass through. Three tests pin the wrapping.
A-5 | IndividualReport.gs, ir-send-to-agent.test.js | `IR_EMAIL_IMAGE_MAX_BYTES_`=8 MB; the payload must match `^data:image/png;base64,` and decode to PNG magic bytes (`irLooksLikePng_`); `irSanitizeDateLabel_` flattens CR/LF/control chars and caps at 120; `irRegisteredAgentEmail_` throws on a read error ("Could not read Access Control…") instead of returning ''. The suite's fixture became a real PNG; four tests added.
A-6 | Auth.gs, shim.js, access-control-editor.test.js | `getAccessEntries_` takes `LockService.getScriptLock().tryLock(AUTH_READ_LOCK_WAIT_MS_=10000)` around the sheet read (`acReadEntriesUncached_(…, mayCache)`); the cache is written only when the lock was held. Shim: `state.lockBusy` makes tryLock return false (waitLock throws). Two tests: locked read caches; busy-lock read serves without caching (positive and `__none__` shapes).

TEST RESULTS: `TZ=America/Chicago node --test` 1686/1686 passed (16 added). `npm run lint:gas` clean. INV-16 guard clean. `node scripts/module-deps.mjs --check` up to date (regenerated: Auth.gs / IndividualReport.gs symbols). `npm run ci:ui`: the T-9 stage PASSED on both roles (overview 1, dept 3/2, IR 1 drawn); the full gate PASSED (all eight stages, exit 0). Six new pins bite-checked: T-7 (manifest scope swapped), A-4 (notes unwrapped), A-5 (PNG check bypassed), A-6 (lock skipped), T-5 (CI=1 dropped from the lint line), T-8 (weekend gate removed): all BITE.
REGRESSION RISKS:
- T-7: the first push of each project after this change alters the consent set; until the owner re-consents (one editor run per project, Operator State #9), a trigger run can fail with "Insufficient permissions". cdr-report's `drive.readonly` is derived from the export fetch, not observed; if the live token never needed it, the only cost is a wider prompt. Auto-detection previously granted whatever it inferred; an explicit list that omits a scope a future edit needs now fails the test, not production.
- T-5: `scripts/deploy.sh` now REFUSES to push from a box without eslint or playwright (was: skip). `DEPLOY_SKIP_CI=1` remains the bypass; the tools install with `npm ci` and `cd tools/ui-harness && npm i playwright`.
- A-5: a client sending a non-PNG image (none exists -- the canvas export is PNG) or a label over 120 chars is refused / truncated; a read error while resolving an agent's registered address now surfaces as an error instead of silently falling back to a typed address.
- A-6: every UNCACHED auth read (once per email per 60 s) now takes the script lock; a save in flight delays it up to 10 s instead of racing it. Lock contention never denies -- it serves uncached.
- A-4: values already stored raw are not rewritten; only new writes are wrapped (readers see the original string, Sheets' apostrophe is not part of the value).
- T-9: a fixture change that removes a chart from Overview / dept / IR now fails ci:ui with `drawn=N` in the detail -- the intended tripwire.
INVARIANTS AT RISK: INV-01 (the four writers keep their gates; only cell neutralization added), INV-31 (scope lists now explicit -- send_mail present in all three), INV-22 (dqe-report untouched). None violated.
NET SCORE: 3 (T-5: a tool-less box could push what CI rejects; A-6: the false-denial race; A-5: the swallowed read error on the send-to-agent path) − 0 = 3

OPERATOR ACTIONS / DEPLOY:
- After pushing each project, open its Apps Script editor, Run any function once and grant the new consent set (T-7: the dashboard's prompt narrows; cdr-report / cdr-import's become explicit) | BLOCKS DEPLOY: N (needed before the next TRIGGER run in cdr-report / cdr-import succeeds)
- Install eslint (`npm ci`) and playwright (`cd tools/ui-harness && npm i playwright`) on any machine that runs `scripts/deploy.sh`; without them the gate now refuses (T-5) | BLOCKS DEPLOY: Y (for deploy.sh users; `DEPLOY_SKIP_CI=1` is the emergency bypass)
Deploy: Department Dashboard: `clasp push -f` from repo root, then New version in the editor (or `scripts/deploy.sh .`)
Deploy: CDR Import: `cd apps-script/cdr-import && clasp push -f` (manifest only)
Deploy: CDR DQE Pipeline / CDR Reporting Tools: `cd apps-script/cdr-report && clasp push -f` (manifest only)

FOLLOW-ON ITEMS:
- dqe-report still relies on scope auto-detection (frozen, INV-22); if it is ever unfrozen for cleanup, give it an explicit list the same way.
- The IR email's image cap (8 MB) is a guess above the ~0.3-2 MB a rendered report produces; MailApp's own attachment limits are the hard ceiling.
- Batch 9 of the scan plan remains (interface structural: C1-13, C2-10, C2-11, UD-1…UD-10).

DOCUMENTATION UPDATES NEEDED:
- /sync-docs pass: CLAUDE.md's deploy-helper block, Operator State #9 and the fix-history rows landed in-batch; check README's deploy section for the "skips if playwright absent" wording and known-issues for any restated scope list.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
