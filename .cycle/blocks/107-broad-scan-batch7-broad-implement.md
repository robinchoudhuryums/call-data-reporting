---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: P-5 (queueSplitSample.js = the SIXTH hand-mirror: per-queue floor + R18e ext fallback, pinned), DD-6 (sidebar col-G rows 36/40 status gates, pinned both sides), DD-5 (per-AGENT QCD reconciliation in qcdDqeDiagnostic), A-7 (sub-queue expansion fails closed for real on an errored Dept Config read), A-8 (Operator State #63 / INV-01 telemetry note / #46 wording), P-10 (INV-44 `outboundExport`; inbound-history horizon reconciled in three places), O-7 (INV-31 -> `sendAppEmail_`), T-1 (`computeDigestStats_`), T-2 (THREE CI jobs; ci.mjs eight stages), T-4 (fix-history entries for F24, F-13, R27 x2, R28, R29, R30), T-10 (~115 suites / "+103 more"), T-3 (module-dependencies.md regenerated; `--check` in the CI test job + `npm run ci`), T-6 (five largest bullets trimmed into fix-history; stale size figures fixed; 90% warning diagnostic in claude-md-split), C1-11 (four SWR slots), C1-12 (amber pill wording x3; sub-queues toggle ON by default), OD-8 (`setup()` heals blank header cells via `healSheetHeaders_`), DD-8 (slot-preview number-format write documented)
Files modified: apps-script/cdr-import/queueSplitSample.js, apps-script/cdr-import/qcdDqeDiagnostic.js, apps-script/cdr-import/inboundCalls.js, apps-script/cdr-report/dataFilters.js, apps-script/cdr-report/sheetRepairs.js, apps-script/department-dashboard/DeptConfig.gs, apps-script/department-dashboard/Setup.gs, apps-script/department-dashboard/script-1-core.html, apps-script/department-dashboard/script-3-overview.html, apps-script/department-dashboard/styles.html, .github/workflows/ci.yml, package.json, scripts/module-deps.mjs, tools/ui-harness/ci.mjs, CLAUDE.md, docs/fix-history.md, docs/invariants.md, docs/operator-state.md, docs/per-call-capture.md, docs/client-ui-conventions.md, docs/module-dependencies.md, tests/README.md, tests/unit/cross-file-pins.test.js, tests/unit/qcd-dqe-diagnostic.test.js, tests/unit/subqueue-access.test.js, tests/unit/setup.test.js, tests/unit/claude-md-split.test.js, tests/unit/qcd-sidebar-parity.test.js

CHANGES:
P-5 | queueSplitSample.js, cross-file-pins.test.js, CLAUDE.md | `queueNameByExt` built from the day's queue-callee legs + the `CallQueue (ext)` CALLER fallback (recovered count logged); every window test goes through one `inWindow` closure calling `dqeWindowStartForQueue_(l.queueName)`; the work-window log names the early floor + its queue list. R49 pin extended: bare `DQE_WINDOW_START` floor or missing fallback in this file fails. Docblock names it the SIXTH mirror; CLAUDE.md R49 bullet gains one clause.
DD-6 | dataFilters.js, cross-file-pins.test.js, qcd-sidebar-parity.test.js, CLAUDE.md | Row 36 col 7 gains `status !== "3"` (the pipeline's r36_G block gate); row 40 col 7 gains `status === "1"` (nonCsrWaitSumDec accumulates only there). New pin "DD-6 col-G status gates" brace-matches both pipeline blocks and both sidebar rows. Parity-suite scope note + CLAUDE.md exclusion clause updated (the T-1 "since drifted" wording is gone).
DD-5 | qcdDqeDiagnostic.js, qcd-dqe-diagnostic.test.js | `qddAgentKeyOf_` (one agent-key rule, used by the analyzer AND the check); `qddReconcileQcdPerAgent_` runs the REAL `calcQcdReport` on each agent's legs (header + filtered body), busiest-first, cap `QDD_PER_AGENT_RECON_CAP_`=60, returns {checked, unchecked, mismatches[[agent,row,mine,real]]}; `qcdOk = qcdTotalsOk && perAgent.mismatches.length === 0` (verdict shape unchanged); report carries `qcdTotalsReconciled` + `qcdPerAgent`; log line added. Tests: fixture agrees per agent; a compensating swap that leaves totals equal is caught on both agents; the cap + remainder.
A-7 | DeptConfig.gs, subqueue-access.test.js | After `subQueueChildMap_()`, `deptConfigReadFailed_()` true => return the assigned list (the sheet reader never throws; it flagged and served the seed constant). Test: control expands the seeded PAP->Sales edge on a healthy empty read; an errored `getLastRow` yields no expansion. install() resets the flag.
OD-8 | Setup.gs, setup.test.js, docs/invariants.md | `healSheetHeaders_(sheet, headers)`: widen if narrower (REP-10), fill BLANK header cells, never rewrite a non-blank one (reported in `differing`, logged). `ensureSheet_` calls it on the exists path. Three tests (3-header Access Control healed + idempotent; narrow sheet widened; custom label untouched). INV-12 + the setup() docblock say so.
DD-8 | sheetRepairs.js | `previewDqeSlotTimestampRepair` docblock: the one exception to "previews never write" (number formats on K-AC/AF, restored per group; a mid-scan kill leaves a group in the numeric lens; values never touched).
T-3 | docs/module-dependencies.md, ci.yml, package.json, scripts/module-deps.mjs, CLAUDE.md | Regenerated (968 symbols); `node scripts/module-deps.mjs --check` step in the CI `test` job and in `npm run ci`; the script's "NOT wired into CI on purpose" rationale replaced.
T-6 | CLAUDE.md, claude-md-split.test.js, docs/fix-history.md | Cuts (every dropped reference verified to resolve): System Health (three read-only sections compressed, TRUNCATE/VACUUM + workbook-cells parentheticals), Neon write (44 KB bridge cap -> fix-history, P-3/C-1/C-6 parentheticals, P-9 why), Comma-joined (re-render example, commit a350042 archaeology -> fix-history), Force-path (S2-2/R10-5 story, the "used to destroy then throw" narrative), Sub-queue (three narrative parentheticals), plus the R27 measurement, the R40 eight-test note, the F1 20-minute story and the R43 measurement. Two facts relocated under "T-6 relocations" in fix-history. Split guard: `CLAUDE_MD_MAX_BYTES` / `CLAUDE_MD_WARN_RATIO`=0.9 with a `t.diagnostic` past 90%; stale "current ~152 KB" comment and the CLAUDE.md preamble figures corrected. Net: 199,101 -> 198,042 B (the batch also ADDED clauses); largest bullets 4094/4067/4032/3944 -> 3906/3903/3867/3861.
T-1 / T-2 / T-4 / T-10 / C1-11 / C1-12 / A-8 / P-10 / O-7 | CLAUDE.md, ci.mjs, fix-history, tests/README, client-ui-conventions, script-1-core, styles, script-3-overview, operator-state #63/#46/#35, invariants INV-01/INV-31/INV-44, per-call-capture, inboundCalls.js docblock | As listed under "Findings implemented".

TEST RESULTS: `TZ=America/Chicago node --test` 1667/1667 passed (8 added: DD-5 x3, A-7 x1, OD-8 x3, DD-6 pin x1; the P-5 pin extends an existing test). INV-16 guard clean. `npm run lint:gas` clean (75 files, 3 projects). `node scripts/module-deps.mjs --check` up to date. Seven new pins bite-checked (P-5 floor, P-5 fallback, DD-6 row 36, DD-6 row 40, DD-5, A-7, OD-8): all BITE. `npm run ci:ui`: PASSED (all eight stages, TZ=America/Chicago)
REGRESSION RISKS:
- A-7: a transient Dept Config read error now DROPS a parent manager's child-dept access for that request (and the 60 s auth cache), where before it silently served the seed constant's edges. Least-privilege by design (INV-38 says fail closed); the manager sees only their assigned dept until the next resolve. Admins / ALL managers unaffected.
- DD-5: `diagnoseQcdVsDqeForDate` now calls `calcQcdReport` up to 61 times per run (one per agent in the CSR block, cap 60); each re-reads `QCDR Output` A2:B49 + the two named ranges. Editor-run diagnostic only; a day with more than 60 CSR-block agents reports the remainder as unchecked rather than silently skipping.
- DD-6: the sidebar's row 36 / row 40 col-G extractions list FEWER rows (status-3 / non-status-1 waits dropped) -- the pipeline's mean never included them, so the tool now agrees with the number it explains.
- OD-8: `setup()` now writes to the header row of existing managed sheets (blank cells only). A sheet whose header row was deliberately left partly blank gains the schema labels; non-blank labels are never touched (the INV-12 idempotence pin still holds).
- T-3: a `.gs` add/rename now fails CI until `node scripts/module-deps.mjs --write` is run (one command; the doc says so).
- P-5: the sample tool's totals now include the CSR family's 6:00-6:30 legs and ext-recovered legs -- matching the build, which is the tool's whole purpose.
INVARIANTS AT RISK: INV-12 (wording extended to header healing; the never-overwrite-rows rule is unchanged and pinned), INV-38 (fail-closed now true in code), INV-06 (a sixth mirror joins the pin family), INV-01/INV-31/INV-44 (doc corrections only). None violated.
NET SCORE: 3 (P-5: MISMATCH on every CSR-family early leg since R49 deployed; A-7: an errored read expanded from the constant; DD-6: wrong rows listed in the reconciliation tool) − 0 = 3

OPERATOR ACTIONS / DEPLOY:
- Re-run `setup()` once after the dashboard deploy so pre-existing Access Control / Dept Config sheets gain their appended header labels (OD-8; idempotent) | BLOCKS DEPLOY: N
- After adding or renaming a `.gs`, run `node scripts/module-deps.mjs --write` before pushing (CI now checks) | BLOCKS DEPLOY: N
Deploy: Department Dashboard: `clasp push -f` from repo root, then Apps Script editor -> Deploy -> Manage deployments -> pencil -> Version: New version -> Deploy (or `scripts/deploy.sh .`)
Deploy: CDR Import: `cd apps-script/cdr-import && clasp push -f` (queueSplitSample.js, qcdDqeDiagnostic.js, inboundCalls.js docblock)
Deploy: CDR DQE Pipeline / CDR Reporting Tools: `cd apps-script/cdr-report && clasp push -f` (dataFilters.js, sheetRepairs.js docblock)

FOLLOW-ON ITEMS:
- CLAUDE.md is still at ~97% of its 200 KB cap after T-6 (7 KB headroom): every batch that adds a clause must pair it with a cut, or the next F8-style section split is due. Candidates: the Common Gotchas preamble's growth narrative, the deferred-mirror bullet, the Public-write-paths LockService parenthetical.
- queueSplitSample.js has only a SOURCE pin (cross-file-pins); a behavioural test driving it against the real build (the dqe-drilldown-parity shape) would catch a rule the build GAINS.
- `importBulkCSVsFromDrive` stays editor-only ("pending Drive permissions" since the menu item was commented out); Operator State #35 / per-call-capture now say so. Wiring the menu item is an operator decision (Drive scope + re-consent).
- Batches 8–9 of the scan plan remain (T-5, T-7, T-8, T-9, A-4, A-5, A-6; C1-13, C2-10, C2-11, UD-1…UD-10).

DOCUMENTATION UPDATES NEEDED:
- /sync-docs pass over this batch: the fix-history rows and the CLAUDE.md / invariants / operator-state clauses landed in-batch; check docs/known-issues.md + docs/architecture.md for any restated "previews never write" / "setup() leaves existing sheets untouched" / "renderDeptDigestEmail_" wording.
- tests/README.md: no new suites (all tests joined existing files); the "+103 more" count now matches the 114 on disk.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
